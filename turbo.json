{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "check-types": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "persistent": true,
      "cache": false
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/manifest.full.json",
        "$TURBO_ROOT$/manifest.readonly.json",
        "$TURBO_ROOT$/README.md",
        "$TURBO_ROOT$/docs/coverage.md",
        "$TURBO_ROOT$/scripts/gen-docs.ts",
        "$TURBO_ROOT$/scripts/slack-methods.json"
      ]
    }
  },
  "globalEnv": ["SKIP_ENV_VALIDATION", "NODE_ENV"]
}
